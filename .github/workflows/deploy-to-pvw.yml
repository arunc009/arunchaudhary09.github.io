name: Deploy to GitHub Pages
on:
  push:
    branches:
      - dev
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@dev

    - name: Build and Deploy
      uses: arunchaudhary09/arunchaudhary09.github.io@dev
      env:
        ACCESS_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        BRANCH: pvw
        FOLDER: dist
        BUILD_SCRIPT: echo "Passed"
